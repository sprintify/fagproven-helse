{
    "name": "fagproven-helsefag",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "docs:dev": "vitepress dev docs --port 3000",
        "docs:build": "vitepress build docs",
        "docs:serve": "vitepress serve docs --port 5000",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "license": "MIT",
    "devDependencies": {
        "vitepress": "^0.19.2"
    }
}